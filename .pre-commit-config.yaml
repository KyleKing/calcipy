---
# Common commands:
# prek install -f
# prek autoupdate
# prek run --all-files mdformat
# prek run --all-files --hook-stage pre-commit
# prek run --all-files --hook-stage pre-push
# prek run --last-commit
# uv run calcipy lint.prek --no-update
#
# See https://pre-commit.com for more information
#  and https://pre-commit.com/hooks.html for more hooks
# And: https://prek.j178.dev/builtin

# Only commit is installed by default: https://pre-commit.com/#pre-commit-install
# Pending rename of pre-push from: https://github.com/pre-commit/pre-commit/issues/2732
default_install_hook_types: ["commit-msg", "pre-commit", "pre-push"]

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-added-large-files
      - id: check-executables-have-shebangs
      - id: check-json
      - id: check-merge-conflict
      - id: check-symlinks
      - id: check-toml
      - id: check-vcs-permalinks
      - id: check-yaml
        args: [--unsafe]
      - id: debug-statements
      - id: destroyed-symlinks
      - id: detect-private-key
      - id: double-quote-string-fixer
      - id: end-of-file-fixer
        exclude: \.copier-answers(\..+)?\.yml|__snapshots__/.*\.ambr
      - id: fix-byte-order-marker
      - id: forbid-new-submodules
      - id: mixed-line-ending
        args: [--fix=auto]
      - id: trailing-whitespace
        exclude: __snapshots__/.*\.ambr
  - repo: https://github.com/commitizen-tools/commitizen
    rev: v4.17.0
    hooks:
      - id: commitizen
  - repo: https://github.com/executablebooks/mdformat
    rev: 1.0.0
    hooks:
      - id: mdformat
        additional_dependencies:
          - "mdformat-gfm-alerts>=2.0.1"
          - "mdformat-mkdocs[recommended]>=5.2.2rc0"
          - "mdformat-slw>=0.4.0"
        exclude: _.+\.md|CHANGELOG\.md|CODE_TAG_SUMMARY\.md
        stages: ["pre-commit"]
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.11.0.1
    hooks:
      - id: shellcheck
        args: [--severity=warning]
        exclude: \.jinja$
        stages: ["pre-commit"]
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: "v4.0.0-alpha.8"
    hooks:
      - id: prettier
        additional_dependencies:
          # Note: this version must be the same as the hook revision
          - "prettier@4.0.0-alpha.8"
          - "prettier-plugin-sh"
        exclude: \.copier-answers(\..+)?\.yml|tests/.*/cassettes/.*\.yaml|__snapshots__/.*\.json
        types_or: [css, html, javascript, json, shell, yaml]
        stages: ["pre-commit"]
  - repo: https://github.com/pappasam/toml-sort
    rev: v0.24.4
    hooks:
      - id: toml-sort-fix
        exclude: uv\.lock
        stages: ["pre-commit"]
  - repo: local
    hooks:
      - id: copier-forbidden-files
        entry: |
          Found copier update rejection files; review them and remove them
          (https://copier.readthedocs.io/en/stable/updating/)
        files: \.rej$
        language: fail
        name: Copier Forbidden Files
      - id: lint-fix
        description: "Run ruff and apply fixes"
        entry: uv run calcipy-lint lint.fix
        language: system
        name: Lint-Fix
        require_serial: true
        stages: ["pre-commit"]
        types: [python]
      - id: prose
        description: "(beta) Run vale with the ai-tells style to flag AI-generated prose tells. Requires vale installed separately"
        entry: uv run calcipy-lint lint.prose
        language: system
        name: Prose
        pass_filenames: false
        require_serial: true
        stages: ["manual"]
      - id: tags
        description: "Create a `CODE_TAG_SUMMARY.md` with a table for TODO- and FIXME-style code comments"
        entry: uv run calcipy-tags tags
        language: system
        name: Tags
        pass_filenames: false
        require_serial: true
        stages: ["pre-push"]
